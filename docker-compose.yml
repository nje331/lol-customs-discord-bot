services:
  lol-bot:
    build: .
    container_name: lol-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Bind mount: writes directly to ./data/ on your server.
      # This folder is never touched by "docker compose down" — data is always safe.
      - ./data:/app/data